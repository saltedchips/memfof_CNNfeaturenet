services:
  memfof-tester:
    build: .
    container_name: memfof-tester
    privileged: true
    shm_size: 4g
    mem_limit: 8g
    environment:
      - HSA_OVERRIDE_GFX_VERSION=10.3.0
    volumes:
      - .:/workspace
    working_dir: /workspace
    command: tail -f /dev/null